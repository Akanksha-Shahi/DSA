// Count partitions with given Difference
int countPartition(vector<int>& arr, int d){
    int totsum = accumulate(arr.begin(), arr.end(), 0);
    if((totSum+d) % 2 != 0 || d>totSum) return 0;
    int K = (totSum+d)/2;
    vector<int> dp(K+1, 0);
    dp[0]=1;
    if(arr[0] <= K) dp[arr[0]]+=1;
    for( int i =1; i<arr.size(); i++){
        vector<int> curr(K+1, 0);
        curr[0]=1;
        for( int t =0; t<=k; t++){
            itn notTake = dp[t];
            int take =0;
            if(arr[i] <= t) take = dp[t-arr[i]];
            curr[t] = take+notTake;
        }
        dp = curr;
    }
    return dp[K];
}
